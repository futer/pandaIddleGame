export function createLoginPage() {
    const body = document.body;

    const email_input = document.createElement('input');
    email_input.className = 'email';
    
    body.appendChild(email_input);
}

